import { Text } from "native-base";
import { SafeAreaView } from "react-native-safe-area-context";

export default function SignSpecs () {
  return (
    <SafeAreaView>
      <Text>SignSpecs</Text>
    </SafeAreaView>
  )
}