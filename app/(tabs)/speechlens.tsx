import { Text, View } from "native-base";
import { SafeAreaView } from "react-native-safe-area-context";

export default function Speechlens () {
  return (
    <SafeAreaView>
      <View>
        <Text>Speechlens</Text>
      </View>
    </SafeAreaView>
  )
}